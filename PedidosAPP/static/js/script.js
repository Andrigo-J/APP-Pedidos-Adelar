function main() {
    addEventListener
    function addRow(){
        const addItens = document.querySelectorAll('.addItens')
        const products = document.querySelectorAll('#nome')
        const qtd = document.querySelectorAll('#qtd')

        return console.log(addItens, products, qtd)
    }
}